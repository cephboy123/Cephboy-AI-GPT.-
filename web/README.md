# Web app (Next.js)

This folder contains the web UI ready to be deployed on Vercel.

Quick start:

cd web
npm install
npm run dev

Build & deploy with Vercel as usual (set CEPHBOY_OPENAI_KEY in Environment Variables).
